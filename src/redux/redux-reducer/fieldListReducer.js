const initState = {
    searchResult: {}
}

const fieldListReducer = (state = initState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default fieldListReducer;